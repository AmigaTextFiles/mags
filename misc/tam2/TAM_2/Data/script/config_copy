requestfile >env:cips title "Dove vuoi copiare?" drawersonly
if not warn
copy >NIL: tam2:data/regalissimi/imagine.config $cips
if not warn
requestchoice >NIL: title "Copia eseguita!!!" body "Il config di Imagine è stato copiato!!!" "Molto bene!!!"
endif
delete >NIL: env:cips
endif
